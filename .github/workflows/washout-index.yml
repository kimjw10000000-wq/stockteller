# Dump-index samples are written by the operator PC poller, not GitHub.
# Keep this file for a manual run only.

name: washout-index

on:
  workflow_dispatch: {}

jobs:
  sample:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Disabled
        run: |
          echo "Washout index capture runs on the operator PC, not GitHub Actions."
          exit 0
