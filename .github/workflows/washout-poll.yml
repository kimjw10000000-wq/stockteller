# Dump-index live capture is the operator PC (poll:washout), not GitHub.
# Keep this file for a manual run only.

name: washout-poll

on:
  workflow_dispatch: {}

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Disabled
        run: |
          echo "Live dump-index capture runs on the operator PC, not GitHub Actions."
          exit 0
